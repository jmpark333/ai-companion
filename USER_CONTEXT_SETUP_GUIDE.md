# 개인 컨텍스트 관리 기능 설정 가이드

이 가이드는 AI Companion 애플리케이션의 개인 컨텍스트 관리 기능을 설정하는 방법을 안내합니다.

## 1. Supabase 데이터베이스 설정

### 1.1 SQL 파일 실행

Supabase 프로젝트의 SQL 편집기에서 `supabase-user-contexts-setup.sql` 파일의 내용을 실행하세요.

1. [Supabase 대시보드](https://app.supabase.com)에 로그인합니다.
2. 프로젝트를 선택합니다.
3. 왼쪽 메뉴에서 **SQL Editor**를 클릭합니다.
4. **New query**를 클릭합니다.
5. `supabase-user-contexts-setup.sql` 파일의 내용을 복사하여 붙여넣습니다.
6. **Run** 버튼을 클릭하여 SQL을 실행합니다.

### 1.2 생성되는 테이블과 함수

SQL 실행 후 다음 테이블과 함수가 생성됩니다:

- **테이블**: `user_contexts`
  - 사용자 컨텍스트 정보를 저장합니다.
  - 필드: id, user_id, context_type, context_data, created_at, updated_at

- **함수**:
  - `get_user_context`: 사용자 컨텍스트 조회
  - `save_user_context`: 사용자 컨텍스트 저장 또는 업데이트
  - `delete_user_context`: 사용자 컨텍스트 삭제

## 2. Netlify Functions 설정

### 2.1 환경 변수 확인

Netlify Functions가 Supabase에 접근할 수 있도록 환경 변수가 설정되어 있는지 확인하세요.

1. [Netlify 대시보드](https://app.netlify.com)에 로그인합니다.
2. 사이트를 선택합니다.
3. **Site settings** > **Build & deploy** > **Environment**로 이동합니다.
4. 다음 환경 변수가 설정되어 있는지 확인합니다:
   - `SUPABASE_URL`: Supabase 프로젝트 URL
   - `SUPABASE_SERVICE_KEY`: Supabase 서비스 키

### 2.2 Functions 배포

`netlify/functions/memory.js` 파일이 이미 업데이트되어 있어야 합니다. 변경 사항이 있다면 다시 배포하세요.

## 3. 애플리케이션 사용법

### 3.1 개인 컨텍스트 설정

1. AI Companion 애플리케이션에서 설정 버튼(⚙️)을 클릭합니다.
2. "🧠 개인 컨텍스트 관리" 섹션까지 스크롤합니다.
3. 텍스트 영역에 개인 정보를 입력합니다:
   - 이름, 나이, 성별
   - 직업
   - 가족 상황
   - 성격
   - 관심사
   - 현재 고민
   - 건강 상태
   - 생활 패턴 등

4. **💾 컨텍스트 저장** 버튼을 클릭하여 저장합니다.

### 3.2 컨텍스트 활용

- 저장된 컨텍스트는 AI가 대화할 때마다 참조하여 더 개인화된 응답을 제공합니다.
- 컨텍스트는 자동으로 30초마다 저장됩니다.
- **📥 컨텍스트 불러오기** 버튼으로 저장된 컨텍스트를 불러올 수 있습니다.
- **🗑️ 컨텍스트 삭제** 버튼으로 컨텍스트를 삭제할 수 있습니다.

## 4. 문제 해결

### 4.1 컨텍스트가 저장되지 않을 경우

1. Netlify Functions 로그를 확인하세요.
2. Supabase 연결 상태를 확인하세요.
3. 브라우저 콘솔에서 오류 메시지를 확인하세요.

### 4.2 AI가 컨텍스트를 참조하지 않을 경우

1. 컨텍스트가 올바르게 저장되었는지 확인하세요.
2. 페이지를 새로고침하여 컨텍스트를 다시 로드하세요.
3. AI 응답 생성 시 컨텍스트가 포함되는지 확인하세요.

## 5. 추가 기능

### 5.1 자동 저장

- 컨텍스트는 30초마다 자동으로 저장됩니다.
- 변경 사항이 있을 때만 저장됩니다.

### 5.2 문자 수 제한

- 컨텍스트는 최대 2000자까지 입력할 수 있습니다.
- 문자 수에 따라 색상이 변경됩니다:
  - 1500자 이하: 기본 색상
  - 1501-1800자: 주황색
  - 1801자 이상: 빨간색

### 5.3 AI 알림

- 컨텍스트 저장/로드/삭제 시 AI가 알림 메시지를 표시합니다.
- 컨텍스트가 없는 경우 10번째 대화마다 설정 안내 메시지가 표시됩니다.

## 6. 보안 고려사항

- 개인 컨텍스트는 민감한 정보를 포함할 수 있으므로 주의해서 처리하세요.
- Supabase RLS(Row Level Security) 정책이 적용되어 있어 사용자는 자신의 컨텍스트만 접근할 수 있습니다.
- API 키는 안전하게 보관하고 공개하지 마세요.